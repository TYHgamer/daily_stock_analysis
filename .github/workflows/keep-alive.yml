name: keep-alive

on:
  schedule:
    - cron: '17 3 1 * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Empty commit and push
        run: |
          git config user.name "TYHgame"
          git config user.email "980186584@qq.com"
          git commit --allow-empty -m "chore: keep repository active [skip ci]"
          git push